package com.github.winexp.battlegrounds.client.util;

import net.minecraft.util.Identifier;

public class ClientConstants {
    public static Identifier VOTE_PACKET_ID = new Identifier("battlegrounds", "vote_packet");
}
